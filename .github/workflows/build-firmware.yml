name: Build firmware

on:
  workflow_dispatch:
    inputs:
      meshcore_ref:
        description: "MeshCore branch, tag, or commit to build from"
        required: true
        default: "main"
      target_env:
        description: "PlatformIO environment"
        required: true
        default: "RAK_4631_companion_repeater_eth_ble"
  push:
    paths:
      - "meshcore_overlay/**"
      - "scripts/**"
      - ".github/workflows/build-firmware.yml"
      - "README.md"
      - "docs/**"
    tags:
      - "v*"
      - "latest"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout overlay repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install PlatformIO
        run: python -m pip install --upgrade platformio

      - name: Build firmware
        env:
          MESHCORE_REF: ${{ github.event.inputs.meshcore_ref || 'main' }}
          TARGET_ENV: ${{ github.event.inputs.target_env || (github.ref_name == 'v0.1.6-static-eth-debug' && 'RAK_4631_companion_repeater_eth_ble_staticdebug' || ((github.ref_name == 'v0.1.5-dual-poll-eth-debug' || github.ref_name == 'v0.1.4-eth-debug') && 'RAK_4631_companion_repeater_eth_ble_ethdebug' || (github.ref_name == 'v0.1.2-ble-debug' && 'RAK_4631_companion_ble_overlay_debug' || 'RAK_4631_companion_repeater_eth_ble'))) }}
        run: bash scripts/build_firmware.sh

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.inputs.target_env || (github.ref_name == 'v0.1.6-static-eth-debug' && 'RAK_4631_companion_repeater_eth_ble_staticdebug' || ((github.ref_name == 'v0.1.5-dual-poll-eth-debug' || github.ref_name == 'v0.1.4-eth-debug') && 'RAK_4631_companion_repeater_eth_ble_ethdebug' || (github.ref_name == 'v0.1.2-ble-debug' && 'RAK_4631_companion_ble_overlay_debug' || 'RAK_4631_companion_repeater_eth_ble'))) }}
          path: dist/**
          if-no-files-found: error

      - name: Publish GitHub release assets
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/**
          generate_release_notes: true
